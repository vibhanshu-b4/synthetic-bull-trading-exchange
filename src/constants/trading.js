/**
 * @file trading.js
 * @description Domain constants for the trading terminal: order types, order sides, and
 * default simulation parameters such as starting capital.
 * @exports ORDER_TYPES, ORDER_SIDES, DEFAULT_CAPITAL, TICK_INTERVALS
 * @note Changing DEFAULT_CAPITAL here affects the portfolio bootstrap; coordinate with backend.
 */

export const ORDER_TYPES = {};

export const ORDER_SIDES = {};

export const DEFAULT_CAPITAL = 0;

export const TICK_INTERVALS = {};
