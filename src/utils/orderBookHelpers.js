/**
 * @file orderBookHelpers.js
 * @description Utility functions for order book data processing: aggregating price levels
 * into depth buckets, computing the mid-price, and calculating the bid-ask spread.
 * @exports aggregateDepth, computeMidPrice, computeSpread
 * @note aggregateDepth is the hot path at high WS frequency — keep it allocation-light.
 */

export const aggregateDepth = () => [];

export const computeMidPrice = () => 0;

export const computeSpread = () => 0;
