/**
 * @file formatters.js
 * @description Pure utility functions for formatting numbers as prices, volumes,
 * currency strings, and percentages in the trading terminal UI.
 * @exports formatPrice, formatVolume, formatCurrency, formatPercent
 * @note All functions are side-effect free and safe to call during render.
 */

export const formatPrice = () => '';

export const formatVolume = () => '';

export const formatCurrency = () => '';

export const formatPercent = () => '';
