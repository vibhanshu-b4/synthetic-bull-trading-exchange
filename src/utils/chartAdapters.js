/**
 * @file chartAdapters.js
 * @description Converts raw trade/tick data from the WebSocket into the CandlestickData
 * and HistogramData formats expected by the lightweight-charts library.
 * @exports toOHLCV, toVolumeBar, toLineSeries
 * @note lightweight-charts requires time values as UTC seconds (Unix timestamp / 1000).
 */

export const toOHLCV = () => ({});

export const toVolumeBar = () => ({});

export const toLineSeries = () => ({});
