/**
 * @file NotFound.jsx
 * @description 404 fallback page rendered by React Router when no route matches.
 * Displays a minimal error message with a link back to the trading terminal.
 * @exports default NotFound  React component
 */

export default function NotFound() {
  return null;
}
