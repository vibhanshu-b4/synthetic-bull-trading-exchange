/**
 * @file RootLayout.jsx
 * @description Top-level application shell providing the persistent navigation bar and
 * the main content area where page-level routes are rendered.
 * @exports default RootLayout  React component
 * @note Renders the WebSocket status indicator in the navbar via useWebSocket().status.
 */

export default function RootLayout() {
  return null;
}
