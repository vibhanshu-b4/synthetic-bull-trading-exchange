/**
 * @file Card.jsx
 * @description Dark-themed panel container with a subtle border and surface-raised background.
 * Used as the visual wrapper for every terminal widget (order book, chart, portfolio, etc.).
 * @exports default Card  React component — accepts `title`, `className`, and `children` props.
 */

export default function Card() {
  return null;
}
