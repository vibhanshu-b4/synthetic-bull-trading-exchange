/**
 * @file Spinner.jsx
 * @description Lightweight SVG loading spinner for indicating async operations
 * (WebSocket reconnecting, order submission, data fetching).
 * @exports default Spinner  React component — accepts `size` and `className` props.
 */

export default function Spinner() {
  return null;
}
