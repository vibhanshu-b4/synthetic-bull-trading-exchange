/**
 * @file Select.jsx
 * @description Styled native <select> wrapper for dropdown choices (e.g. order side, asset symbol).
 * Applies dark-theme appearance overrides to counteract browser defaults.
 * @exports default Select  React component
 */

export default function Select() {
  return null;
}
