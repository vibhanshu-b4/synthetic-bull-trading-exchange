/**
 * @file Badge.jsx
 * @description Color-coded status badge for displaying categorical states such as
 * order side (BUY=green / SELL=red), order status (FILLED, OPEN, CANCELLED), and bot state.
 * @exports default Badge  React component — accepts `variant` and `label` props.
 */

export default function Badge() {
  return null;
}
