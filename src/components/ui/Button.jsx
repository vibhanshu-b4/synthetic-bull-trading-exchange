/**
 * @file Button.jsx
 * @description Headless, theme-aware button primitive supporting variant (primary, ghost, danger)
 * and size (sm, md, lg) props with full Tailwind class composition.
 * @exports default Button  React component
 * @note Use the `asChild` pattern in future if Radix UI is adopted; keep this file logic-free.
 */

export default function Button() {
  return null;
}
