/**
 * @file BotCard.jsx
 * @description Individual bot status card displaying the bot's name, operational status
 * (RUNNING / PAUSED / ERROR), cumulative P&L, and a preview of its most recent trades.
 * @exports default BotCard  React component — accepts `botId` prop; reads from useBotStore.
 */

export default function BotCard() {
  return null;
}
