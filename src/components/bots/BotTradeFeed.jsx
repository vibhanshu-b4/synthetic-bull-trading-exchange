/**
 * @file BotTradeFeed.jsx
 * @description Auto-scrolling live feed of trades executed by a specific bot, newest entry at top.
 * Each row shows timestamp, side, price, and quantity.
 * @exports default BotTradeFeed  React component — accepts `botId` prop.
 * @note Cap rendered rows to 50 max to avoid unbounded DOM growth; virtual scroll if needed later.
 */

export default function BotTradeFeed() {
  return null;
}
