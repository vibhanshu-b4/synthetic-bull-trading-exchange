/**
 * @file BotStatusPanel.jsx
 * @description Container panel that renders a BotCard for each configured autonomous trading bot
 * (Market Maker Bot, Alpha Bot) and aggregates their combined P&L summary.
 * @exports default BotStatusPanel  React component
 * @note Bot data is optional — render nothing if botStore is empty rather than showing skeletons.
 */

export default function BotStatusPanel() {
  return null;
}
