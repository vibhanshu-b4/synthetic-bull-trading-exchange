/**
 * @file PnLBadge.jsx
 * @description Displays a P&L value with automatic green/red colouring and a directional arrow.
 * Accepts both absolute (currency) and relative (%) P&L; renders both when both are provided.
 * @exports default PnLBadge  React component — accepts `value`, `percent`, and `currency` props.
 */

export default function PnLBadge() {
  return null;
}
