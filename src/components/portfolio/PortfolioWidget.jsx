/**
 * @file PortfolioWidget.jsx
 * @description Summary card displaying the user's available cash, total holdings value,
 * total equity, and overall P&L sourced from usePortfolio.
 * @exports default PortfolioWidget  React component
 * @note Throttle renders via useThrottle on equity to avoid jitter at high WS update rates.
 */

export default function PortfolioWidget() {
  return null;
}
