/**
 * @file HoldingsTable.jsx
 * @description Per-asset holdings table showing symbol, quantity, average cost basis,
 * current live price, market value, and unrealised P&L for each position.
 * @exports default HoldingsTable  React component
 */

export default function HoldingsTable() {
  return null;
}
