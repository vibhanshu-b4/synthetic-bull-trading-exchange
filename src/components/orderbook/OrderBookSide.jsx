/**
 * @file OrderBookSide.jsx
 * @description Renders a single side (bids or asks) of the order book as a fixed-height
 * virtualized list with price, size, and cumulative depth bar visualisation.
 * @exports default OrderBookSide  React component — accepts `side`, `levels`, and `maxDepth` props.
 * @note Use CSS-only depth bars (width % via inline style) rather than a charting lib to keep renders fast.
 */

export default function OrderBookSide() {
  return null;
}
