/**
 * @file SpreadIndicator.jsx
 * @description Displays the current bid-ask spread (absolute and percentage) and mid-price
 * in a compact row between the two order book sides.
 * @exports default SpreadIndicator  React component — accepts `spread`, `midPrice`, and `spreadPct` props.
 */

export default function SpreadIndicator() {
  return null;
}
