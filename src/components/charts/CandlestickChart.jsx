/**
 * @file CandlestickChart.jsx
 * @description lightweight-charts wrapper that initialises a candlestick series on mount and
 * calls series.update() for each new candle received from useCandlestick, never full re-renders.
 * @exports default CandlestickChart  React component — accepts `interval` and `symbol` props.
 * @note The chart instance is stored in a ref; never put it in React state to avoid reconciliation.
 */

export default function CandlestickChart() {
  return null;
}
