/**
 * @file PriceLine.jsx
 * @description Horizontal price line overlay for the candlestick chart — renders the user's
 * open limit order prices as named dashed lines via the lightweight-charts createPriceLine API.
 * @exports default PriceLine  React component — accepts `price`, `label`, and `color` props.
 * @note Rendered as a lightweight-charts API call, not as a DOM element; component is headless.
 */

export default function PriceLine() {
  return null;
}
