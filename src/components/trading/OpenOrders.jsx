/**
 * @file OpenOrders.jsx
 * @description Table listing the user's currently open (unfilled) orders with columns for
 * side, type, price, quantity, and a cancel action button per row.
 * @exports default OpenOrders  React component
 * @note Cancel triggers cancelOrder REST call + optimistic removal from orderStore.
 */

export default function OpenOrders() {
  return null;
}
