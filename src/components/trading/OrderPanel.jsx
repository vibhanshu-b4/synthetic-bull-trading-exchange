/**
 * @file OrderPanel.jsx
 * @description Tabbed container that switches between MarketOrderForm and LimitOrderForm.
 * Also renders the OpenOrders table beneath the active form.
 * @exports default OrderPanel  React component
 * @note Keep tab state local (useState); it does not need to be in the global Zustand store.
 */

export default function OrderPanel() {
  return null;
}
