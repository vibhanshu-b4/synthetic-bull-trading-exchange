/**
 * @file MarketOrderForm.jsx
 * @description Form for submitting a market order: collects side (BUY/SELL) and quantity,
 * then calls useOrderSubmit with ORDER_TYPES.MARKET.
 * @exports default MarketOrderForm  React component
 * @note Displays estimated cost (quantity × last price) as a live preview before submission.
 */

export default function MarketOrderForm() {
  return null;
}
