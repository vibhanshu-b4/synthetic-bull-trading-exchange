/**
 * @file LimitOrderForm.jsx
 * @description Form for submitting a limit order: collects side, quantity, and limit price,
 * then calls useOrderSubmit with ORDER_TYPES.LIMIT.
 * @exports default LimitOrderForm  React component
 * @note Pre-fills the price field with the current best bid or ask based on selected side.
 */

export default function LimitOrderForm() {
  return null;
}
