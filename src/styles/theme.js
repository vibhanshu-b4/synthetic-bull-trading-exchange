/**
 * @file theme.js
 * @description NextBull color palette, font scales, and spacing tokens exported as plain JS objects.
 * Consumed by tailwind.config.js for design-token consistency and directly by charting libraries
 * that cannot read CSS variables (e.g. lightweight-charts series color options).
 * @exports colors, fontFamily, fontSize
 */

export const colors = {};

export const fontFamily = {};

export const fontSize = {};
